{
  "name": "monthly-wage-calculator",
  "description": "Initializer",
  "engines": {
    "node": "6.9.2"
  },
  "scripts": {
    "postinstall": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start",
    "test": "cd client && npm test && cd ../server && npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tlcada/monthly-wage-calculator.git"
  }
}